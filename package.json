{
  "name": "@cometu/space",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "lint": "eslint . --cache"
  },
  "devDependencies": {
    "@kecrily/eslint-config": "^0.1.20",
    "eslint": "^8.26.0",
    "typescript": "^4.8.4"
  },
  "eslintConfig": {
    "extends": "@kecrily",
    "ignorePatterns": [
      "!.vitepress"
    ]
  }
}
